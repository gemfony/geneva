set(OAINCUBATOR_HEADERS
        incubator.hpp
)

set(OAINCUBATOR_SOURCES
        incubator.cpp
)

set(LIBRARYNAME
	incubator
)

add_library(${LIBRARYNAME} SHARED
	${OAINCUBATOR_HEADERS}
	${OAINCUBATOR_SOURCES}
)

INSTALL (TARGETS ${LIBRARYNAME}
	DESTINATION /opt/${EXECUTABLENAME}/libs
)
