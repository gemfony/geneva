CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET ( HAPTESTINCLUDES
    GHap_tests.hpp
)

# This is a workaround for a CLion-problem -- see CPP270 in the JetBrains issue tracker
ADD_CUSTOM_TARGET(CLION_ALL_HAP_TEST_HEADERS SOURCES ${HAPTESTINCLUDES})

INSTALL ( FILES ${HAPTESTINCLUDES} DESTINATION ${INSTALL_PREFIX_INCLUDES}/hap/tests )
