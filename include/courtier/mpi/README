This directory will contain a consumer and client infrastructure that allows to
use Geneva in an MPI environment. The code is built as a separate library,
so users of the Courtier library are not forced to build all of Geneva with
MPI, if they do not need it.
