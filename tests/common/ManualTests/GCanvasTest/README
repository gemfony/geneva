This directory contains a manual test / usage demo of the GCanvas class,
which lets you load, store (and to some extent modify) images in PPM format.
Here, we add a triangle to a picture of the Mona Lisa. This is useful for 
some demos we are preparing.