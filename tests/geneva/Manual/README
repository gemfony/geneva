This directory contains tests that mostly require manual inspection of the
results. As an example, the GBooleanProbabilityTest test creates plots of
flip ratio probabilities which can be viewed with the Root analysis toolkit.
