CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

ADD_CUSTOM_TARGET( "examples-courtier"
	# Add all the available example's targets here
	DEPENDS
		# No subtargets at the moment
	COMMENT "Building all the examples for the Courtier library."
)

#ADD_SUBDIRECTORY( )
