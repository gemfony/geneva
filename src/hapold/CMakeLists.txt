CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

SET ( HAPSOURCES
	GHapEnums
	GRandomBase
	GRandomFactory
	GSeedManager
)

# BUILD_SHARED_LIBS controls the behavior of ADD_LIBRARY if STATIC/SHARED omitted
IF ( HAP_STATIC )
	ADD_LIBRARY ( ${HAPLIBNAME} STATIC ${HAPSOURCES} )
ELSE ()
	ADD_LIBRARY ( ${HAPLIBNAME} SHARED ${HAPSOURCES} )
ENDIF ( HAP_STATIC )

INSTALL ( TARGETS ${HAPLIBNAME} DESTINATION lib
	PERMISSIONS
	OWNER_READ OWNER_WRITE
	GROUP_READ
	WORLD_READ
)
